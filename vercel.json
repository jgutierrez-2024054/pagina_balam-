{
  "buildCommand": "npx prisma migrate deploy && if [ \"$VERCEL_ENV\" = \"production\" ]; then npm run prisma:seed; fi && npm run postinstall",
  "rewrites": [
    {
      "source": "/api/:match*",
      "destination": "/api/[...catchall]"
    },
    {
      "source": "/uploads/:match*",
      "destination": "/api/[...catchall]"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
